node_modules/
.env
.env.local
dist/

# v2 runtime data — SQLite file + future runtime artifacts
data/
.replit
maddy/
attached_assets/
*.tar.gz
*.backup
.DS_Store
*.log

# SSH keys (if accidentally created in root)
*.pem
*.key

# Local Claude Code agent state (sessions, memory, worktrees)
.claude/

# Dev server local pidfile
tunnel.pid
