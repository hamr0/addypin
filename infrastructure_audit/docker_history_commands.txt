# Check if there's a docker-compose file with PostgreSQL
ls -la /opt/addypin/docker-compose.yml
docker run -d   --name addypin-postgres   -e POSTGRES_DB=addypin   -e POSTGRES_USER=addypin   -e POSTGRES_PASSWORD=addypin_password   -p 5432:5432   postgres:15
# Check the docker-compose file for database credentials
cat /opt/addypin/docker-compose.yml
# Check the docker-compose file for database credentials
cat /opt/addypin/docker-compose.yml
# Check the docker-compose file for database credentials
cat /opt/addypin/docker-compose.yml
# Find other configuration files (docker-compose, configs, etc.)
# Check if any environment variables are set in docker-compose files (without showing secrets)
grep -E "(PORT|NODE_ENV|DATABASE)" /opt/addypin/docker-compose.yml > docker_env_vars.txt 2>/dev/null
grep -E "(PORT|NODE_ENV|DATABASE)" /opt/addypin-staging/docker-compose.yml >> docker_env_vars.txt 2>/dev/null
